<h1>Memo#New</h1>

<div class="row">
  <%= form_for(@memo, url: memos_path) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %><br>

    <%= f.label :body %>
    <%= f.text_area :body %>

    <%= f.submit "Create new memo", class: "btn btn-primary" %>
  <%  end %>
  <%= link_to 'Back', users_path %>
</div>
