<p>
  ==================================<br>
  <%= "name: #{notice.name}" %><br>
  <%= "message: #{notice.message}" %><br>
  <%= "created at: #{format_datetime(notice.created_at)}"  %>
  <% unless notice.has_read %>
    <%= button_to 'Mark as Read', notice_mark_as_read_path(id: notice)%>
  <% end %>
  ==================================<bra>
</p>
