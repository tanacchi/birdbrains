<h1>新規登録</h1>

<div class="container">
  <div class="form-group col-xs-4">
    <%= form_for(@user, url: users_path) do |f| %>
      <%= f.label :name, '名前' %>
      <%= f.text_field :name, class: 'form-control', placeholder: 'ユーザー名を入力' %><br>

      <%= f.label :email, 'Email' %>
      <%= f.email_field :email, class: 'form-control', placeholder: 'メールアドレスを入力' %><br>

      <%= f.submit '新規登録', class: "btn btn-primary", style: 'background-color: #77d;' %><br>
    <% end %>
    <%= link_to 'トップへ', root_path, class: 'btn btn-primary', style: 'margin-top: 50px; background-color: #77d;' %>
  </div>
</div>
